{
  "_note": "TEMPLATE — the outer schema is spike-pending. Muse Code documents that managed hooks live at the path given by the `managed_hooks_path` setting and that a hook binds to exactly one event, but publishes no schema for this file. Confirm with `muse hooks validate` before shipping. The inner command line is the part this adapter guarantees: the event name is passed as argv, and the adapter also accepts it from the payload.",
  "_placeholder": "${PINTA_MUSECODE_ROOT} is substituted by pinta-manager at enroll time with the adaptor install directory. `node` is rewritten to the manager's bundled Node on macOS; on Linux there is no bundled Node — see README.",
  "_omitted": "PreLLMCall and PostLLMCall are intentionally absent. PostLLMCall is expected to fire per response chunk; add them only after measuring the volume, together with PINTA_MUSE_LLM_EVENTS=1.",
  "hooks": {
    "SessionStart": [
      { "type": "command", "command": "node ${PINTA_MUSECODE_ROOT}/dist/index.js SessionStart" }
    ],
    "UserPromptSubmit": [
      { "type": "command", "command": "node ${PINTA_MUSECODE_ROOT}/dist/index.js UserPromptSubmit" }
    ],
    "PreToolUse": [
      { "type": "command", "command": "node ${PINTA_MUSECODE_ROOT}/dist/index.js PreToolUse" }
    ],
    "PermissionRequest": [
      {
        "type": "command",
        "command": "node ${PINTA_MUSECODE_ROOT}/dist/index.js PermissionRequest"
      }
    ],
    "PostToolUse": [
      { "type": "command", "command": "node ${PINTA_MUSECODE_ROOT}/dist/index.js PostToolUse" }
    ],
    "PreCompact": [
      { "type": "command", "command": "node ${PINTA_MUSECODE_ROOT}/dist/index.js PreCompact" }
    ],
    "PostCompact": [
      { "type": "command", "command": "node ${PINTA_MUSECODE_ROOT}/dist/index.js PostCompact" }
    ],
    "SubagentStart": [
      { "type": "command", "command": "node ${PINTA_MUSECODE_ROOT}/dist/index.js SubagentStart" }
    ],
    "SubagentStop": [
      { "type": "command", "command": "node ${PINTA_MUSECODE_ROOT}/dist/index.js SubagentStop" }
    ],
    "Stop": [{ "type": "command", "command": "node ${PINTA_MUSECODE_ROOT}/dist/index.js Stop" }]
  }
}
