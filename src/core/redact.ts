// Re-exported from @pinta-ai/core — this module is byte-identical across all
// Pinta adapters and lives in the shared package.
export {
  redact,
  truncate,
  collectMatches,
  resolveOverlaps,
  applyMatches,
  PATTERNS,
  MAX_BYTES,
} from "@pinta-ai/core";
export type { RedactOptions, Pattern } from "@pinta-ai/core";
